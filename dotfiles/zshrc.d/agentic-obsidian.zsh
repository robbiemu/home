# --- Agentic automation ---
# Non-secret local paths/commands used by scheduled one-off agent jobs.
export AGENTIC_RUNTIME="${AGENTIC_RUNTIME:-codex}"
export AGENTIC_ONE_OFF_CMD="${AGENTIC_ONE_OFF_CMD:-}"
home_repo_export_dir OBSIDIAN_VAULT_DIR "$HOME/Documents/Obsidian"
